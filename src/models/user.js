// const db = require('../models/connectDB')
// const userSchema = require('../schema/user')

// // 查询用户表信息
// userSchema.static('findUser', async function (params) {
//   return await this.findOne(params).populate('projects').sort('meta.updateAt')
// })

// module.exports = db.model('user', userSchema)